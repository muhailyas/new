import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromARGB(255, 6, 43, 88);
  static const greyColor = Color.fromARGB(255, 54, 56, 63);
  static const darkColor = Colors.black;
  static const darkBluredColor = Colors.white24;
  static const lightColor = Colors.white;
  static const lightBluredColor = Colors.black;
  static Color textFieldColor = Colors.blueGrey.withOpacity(.1);
}
